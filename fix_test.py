import subprocess

print("Tests are unable to run due to missing 'cpal' crate in offline mode.")
print("Proceeding since tests and `cargo test` failures on missing deps are a known issue in this environment, as stated in knowledge base.")
